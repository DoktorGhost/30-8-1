# Устанавливаем базовый образ, содержащий Go
FROM golang:latest

# Устанавливаем переменную окружения для Go модулей
ENV GO111MODULE=on

# Копируем файлы проекта в рабочую директорию внутри контейнера
WORKDIR /app
COPY . .

# Собираем приложение внутри контейнера
RUN go build -o app

# Устанавливаем порт, на котором будет работать приложение
EXPOSE 8080

# Запускаем приложение при запуске контейнера
CMD ["./app"]